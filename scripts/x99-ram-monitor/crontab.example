# Каждые 20 минут с 8:00 до 23:59 — чаще не надо, забанят.
# Установка: crontab -e и вставить строку, поправив пути.
*/20 8-23 * * * cd /path/to/scripts/x99-ram-monitor && TG_BOT_TOKEN=xxx TG_CHAT_ID=yyy .venv/bin/python monitor.py --once >> monitor.log 2>&1
